프로젝트 이름 : rpi-sensor-monitor

프로젝트 개요
- 라즈베리 파이 4B 4GB를 이용하여 여러 센서의 신호를 처리하는 프로그램 만드는게 목표이다.
- UI 부분은 QML과 WPF로 할 예정이며 코어 부분은 C++을 이용해서 만든다.
- 위의 내용을 완성 후 AI 를 이용하여 이상치 탐지 정도의 기능을 붙여보고 싶어.
- 포트폴리오 목적이고, 설계 과정과 결정 이유를 문서로 남긴다.

프로젝트 순서
- 1차 목표
  = C++을 활용한 코어 라이브러리 제작 (수집 -> 큐 ->  처리 -> 결과 전달)
  = 코어를 활용한 QML UI, 라즈베리파이 7인치 터치스크린 동작 제작
- 2차 목표(우선순위 선택)
  = 코어를 활용한 WPF 뷰어 제작
  = 코어 데몬 분리 + IPC 통신
  = AI를 활용한 이상치 탐지

하드웨어
- Raspberry Pi 4B 4GB, 공식 7인치 DSI 터치 디스플레이, Raspberry Pi OS 64-bit
- BME280 (온도·습도·기압, I2C, 느린 신호 ~1 Hz)
- MPU6050 (가속도·자이로, I2C, 빠른 신호 ~100 Hz 이상)
 * 두 센서는 dtoverlay=i2c-gpio로 만든 별도 I2C 버스에 각각 연결

아키텍처 결정
 - 센서별 수집 스레드 -> 센서별 유한 큐 -> 처리 스레드 -> UI
 - 큐 꽉차면 오래된 것 버리고 드롭 카운터
 - 블록 단위 전달, BlockBase 상속 + unique_ptr로 큐에 넣음
 - 처리 순서는 타임스탬프 병합
 - 뮤텍스 하나 + 조건변수 하나로 시작
 - 코어는 C++만 활용

코드 규칙
 - C++17 이상 최신 기능 위주
 - 기능별 파일 분리
 - 코어는 단위 테스트 필수
 - 커밋 및 주석은 한글, 식별자는 영어

Claude 작업 규칙 ( 중요 )
이 프로젝트의 목적은 사용자가 직접 코드를 작성하여 학습이 목표임

하지 말 것
 - 학습 목표인 만큼 직접 개발할 테니 임의로 직접 코드 작성 절대 금지.

해도 되는 것
 - 코드 리뷰 : 문제점, 모던 C++ 관점의 개선점, 스레드 안정성 지적
 - 빌드 및 링크 에러 원인 분석, 요청시 해결 방향 제시
 - CMake 설정 방법 알려주기
 - README 구조 제안
 - 개념 설명 (조건변수, 소유권 이전, ONNX Runtime 등)

응답 방식
 - 요청이 모호하면 추측하지 말고 어떻게 이해했는지 먼저 말한다.
 - 확신이 없으면 없다고 말한다.
 - 트레이드오프와 반대 근거를 같이 제시한다. 동의만 하지 않는다.
 - 묻지 않은 것을 덧붙이지 않는다.